export const author = "Flagway Team";
export const title = "National Tournament 2025—Save the Date 👀";
export const date = "2025-02-07";
import SaveDateImage from "@/public/photos/2025-save-date.png";
import Image from "next/image";

import MdxLayout from "@/components/MdxLayout";

The National Tournament is coming up! We can't wait to see you at the 2025 National Flagway Tournament at the University of San Fransisco, CA from May 30-31.

<Image
  className="mt-5 m-auto w-full md:w-[400px]"
  src={SaveDateImage}
  width="400"
  alt="Save the Date 2025"
/>

export default function Page({ children }) {
  return (
    <MdxLayout title={title} author={author} date={date}>
      {children}
    </MdxLayout>
  );
}
