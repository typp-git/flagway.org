"use client"
// import { motion } from 'framer-motion';
// import Image from 'next/image';
// import Structure from "@/public/clear-structure.png"

export default function Loading() {
  return <div>
		{/* <motion.div
			className="flex items-center justify-center"
			animate={{ rotate: [0, 360, 360, 0] }}
			transition={{ repeat: Infinity, duration: 2.5, times: [0, 0.5, 1, 1.5], ease: "easeInOut" }}
		>
			<Image priority src={Structure} width={200} alt="Flagway structure"/>
		</motion.div> */}
	</div>
}